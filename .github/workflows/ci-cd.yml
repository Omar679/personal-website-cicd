name: CI/CD Pipeline

on:
  push:
    branches: [ main ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Build and push Docker image
      uses: docker/build-push-action@v4
      with:
        context: .
        push: true
        tags: |
          omar679/personal-website:latest
          omar679/personal-website:${{ github.sha }}

    - name: Deploy to Render.com
      if: github.ref == 'refs/heads/main'
      run: |
        curl -X POST "https://api.render.com/v1/services/${{ secrets.RENDER_SERVICE_ID }}/deploys" \
          -H "Accept: application/json" \
          -H "Authorization: Bearer ${{ secrets.RENDER_API_KEY }}" \
          -H "Content-Type: application/json" \
          -d '{"clearCache": "false"}'

  # Optional: You can also rely on Render.com's Docker Hub integration for auto-deploy
  # Uncomment the following job if you prefer to use Render's built-in Docker Hub integration
  # deploy-via-render-integration:
  #   needs: build-and-deploy
  #   runs-on: ubuntu-latest
  #   steps:
  #   - name: Wait for Docker Hub to push (optional sleep)
  #     run: sleep 30
  #   - name: Notify Render.com of new image (if using webhook)
  #     run: |
  #       curl -X POST "${{ secrets.RENDER_WEBHOOK_URL }}" \
  #         -H "Content-Type: application/json" \
  #         -d '{"tag": "latest"}'